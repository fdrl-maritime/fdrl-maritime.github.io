# fdrl-maritime.github.io

Standalone project page for *Falsification-Driven Reinforcement Learning for Maritime Motion Planning*.

Built with [Hugo](https://gohugo.io/) (extended). To run locally:

```sh
hugo server
```

To build the static site to `public/`:

```sh
hugo
```
